import { all } from 'redux-saga/effects';

// import { watcherAuthConfirmEmail } from '@/entities/Auth';

export function* saga() {
  yield all([
    // auth
    // watcherAuthConfirmEmail(),
  ]);
}
